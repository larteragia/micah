# Memoria permanente do projeto Micah.
# LEI: nada entra aqui sem o bloco `prova` — identificador de artefato lido DO AR.
# Fonte da telemetria de achados (secao 4.1 do Quadro de Cards).

projeto:
  nome: Micah
  versao: 0.8.6
  bundle_id: app.orvoton.micah
  origem: fork de crynta/terax-ai, publicado em github.com/larteragia/micah
  caminho: C:\Users\Zigfriad\projetos\micah
  branch_de_trabalho: main
  autor: Rodrigo Campos
  licenca: Apache-2.0

# ---------------------------------------------------------------------------
# LEI ZERO: O PROJETO ESTA FECHADO
# ---------------------------------------------------------------------------
# Ordem do Rodrigo, 2026-08-14. O escopo tecnico do Micah esta definido: a
# stack abaixo E o projeto. O trabalho daqui para frente e FAZER FUNCIONAR o
# que ja esta dentro dele, nao acrescentar tecnologia.
#
# O que isso proibe, sem excecao:
#   - dependencia nova (npm ou cargo), inclusive "so uma pequena"
#   - runtime, framework, banco, servidor ou protocolo novo
#   - trocar uma peca da stack por outra "melhor"
#   - reescrever um subsistema que ja funciona
#
# O que isso NAO proibe (esclarecido pelo Rodrigo, 2026-08-14): declarar como
# dependencia DIRETA uma crate que ja esta compilada dentro do binario por
# vir de dependencia existente. "Fazer funcionar o que tem dentro dele"
# inclui o que ja esta dentro do binario: webview2-com e windows entram via
# wry ha muito tempo — torna-las diretas nao adiciona um byte nem uma
# tecnologia. Exemplo concreto: webview2-com liberado para o painel de
# browser (GetFavicon nativo, listagem/remocao de extensoes, ClearBrowsingData).
#
# O que isso obriga:
#   - resolver com o que ja esta declarado nas listas `stack` abaixo
#   - se parecer que falta uma biblioteca, procurar de novo: quase sempre a
#     capacidade ja esta no bundle (CodeMirror, xterm, radix, zustand,
#     tauri-plugin-store, wry) e o que falta e a fiacao
#   - se realmente nao houver caminho sem dependencia nova, PARAR e escrever o
#     motivo no card. A decisao e do Rodrigo, nunca do executor.

stack:
  runtime:
    - Tauri 2.11.5 (feature `unstable` atras da feature de crate `browser-panel`)
    - wry 0.55.1
    - no Windows a webview e WebView2 (Edge/Chromium); webview2-com 0.38.2 e
      windows 0.61.3 entram por wry, nao sao dependencia direta
    - Rust edition 2021, workspace com crates/micah-cli e crates/micah-control-protocol
  frontend:
    - React 19.2 + TypeScript 6 + Vite 8
    - Tailwind v4 (config em src/App.css via @theme, sem tailwind.config)
    - shadcn/ui (style radix-luma, base mist) + radix-ui 1.6 + hugeicons
    - zustand 5 (estado), react-resizable-panels 4.12 (layout), motion (animacao)
    - sonner 2 (toast), cmdk 1.1 (paleta), streamdown 2.5 (markdown do chat)
    - @tanstack/react-virtual 3.14 (listas virtualizadas)
  terminal:
    - xterm 6 + addons fit / search / serialize / web-links / webgl
    - portable-pty 0.9 no Rust; ConPTY no Windows com SPAWN_LOCK e Job Object
    - LEI DE RECURSO: o pool de renderers WebGL tem teto 5
      (terminal/lib/rendererPool.ts). Nenhuma superficie nova pode pedir um
      contexto WebGL: ela despejaria o renderer de um terminal vivo.
  editor:
    - CodeMirror 6 completo (state, view, language, search, merge, lint,
      autocomplete, vim) com langs js/ts, rust, python, go, css, html, json,
      markdown, php, vue, svelte, legacy-modes
    - temas @uiw/codemirror-theme-* + temas locais em editor/lib/cmThemes.ts
    - codemirror-languageserver 1.22 para LSP
  ai:
    - Vercel AI SDK v6 (`ai` 6.0.x) + @ai-sdk/react
    - providers instalados: anthropic, openai, openai-compatible, google, xai,
      cerebras, groq
    - provider proprio `claude-code` em src/cavalo/claudeCode/ (roda a CLI
      oficial `claude`, sem chave, sem cobranca por token)
    - zod 4 para schema de tool
  persistencia:
    - tauri-plugin-store (micah-spaces.json, micah-ai-sessions.json, settings)
    - localStorage para o que precisa ser lido no primeiro paint (largura e
      modo do painel esquerdo, flag do browser)
    - chaves de API no keychain do SO via keyring 3.6 (servico `micah-ai`)
  plugins_tauri:
    - opener, log, os, store, process, notification, autostart, clipboard-manager,
      updater, window-state
  ferramentas:
    - pnpm 11.9 (nunca npm/npx/yarn)
    - biome 2.5 (lint e format), vitest 4.1 (teste), playwright 1.62 (E2E do
      painel de browser, via CDP)
    - size-limit e knip para orcamento de bundle e codigo morto

leis_do_projeto:
  - O PROJETO ESTA FECHADO (ver LEI ZERO acima)
  - OpenRouter e banido; o provider padrao e claude-code (sem chave, preco fixo)
  - PROIBIDO qualquer LLM com cobranca por uso/token
  - a sidebar de arquivos fica a DIREITA
  - as abas vivem no painel Tabs da sidebar, nao em barra no topo da janela
  - o canto superior esquerdo e o seletor do painel esquerdo, em ordem fixa:
    Browser | Editor | Ai Viewer
  - o Smart App Control do Windows precisa ficar desligado para o binario rodar
  - sem em-dash e sem emoji em codigo, comentario, commit ou doc
  - imports sempre `@/...` no frontend, nunca relativo entre modulos

arquitetura_em_uma_linha: |
  Rust dono de todo acesso ao SO (pty, fs, git, shell, lsp, net, secrets,
  browser), webview so fala por invoke(); frontend em modulos autocontidos sob
  src/modules/<area> com barrel index.ts, e App.tsx apenas coordena.
  A logica nova mora em funcao pura e testada; comando Tauri e componente React
  ficam finos.

verificacao:
  frontend: pnpm lint && pnpm check-types && pnpm test
  rust: cd src-tauri && cargo clippy --all-targets --locked -- -D warnings && cargo test --locked
  rollback: cd src-tauri && cargo check --no-default-features   # sem browser-panel
  build:
    frontend: pnpm build
    binario: pnpm tauri build --no-bundle
    trava: o build falha enquanto micah.exe estiver em execucao (arquivo em uso)
  producao: |
    App desktop. "No ar" = o binario src-tauri/target/release/micah.exe
    reconstruido e em execucao, reportando o mesmo build id publicado
    (comando browser_build_id, de MICAH_BUILD_ID emitido no build.rs).
  prova_de_ui: |
    A UI do Micah NAO e alcancavel por CDP: so a webview-filha do painel de
    browser recebe --remote-debugging-port. Prova de comportamento se faz de
    fora do app, com os tres scripts em scripts/:
      window-shot.ps1   captura a janela (PrintWindow + PW_RENDERFULLCONTENT)
      window-click.ps1  clica em coordenada de cliente (SendInput)
      window-key.ps1    envia atalho de teclado
    Os tres declaram SetProcessDPIAware: sem isso, num monitor a 150% a captura
    vira um recorte do canto superior esquerdo e o clique erra o alvo, os dois
    reportando sucesso.

rondas_pdi:
  - card: micahs-mind-2026-08-17
    titulo: "Micah's Mind: rastro luminoso da AI no lugar do AI Viewer"
    data: 2026-08-18T01:30:00-03:00
    resultado: "deploy done — parser/fold, citymap treemap congelado, cena Canvas 2D (zero dependencia nova), sync por aba, report com juiz GLM gravando no oracle-rag (memoria 108625), demolicao do viewer velho em commit proprio; binario 36412b9 reconstruido, executado e reportando build id no log; provas de janela dos criterios 1 e 5 colhidas com o comandante usando o app ao vivo (criterios 2/3/4 pendentes de janela livre)"
    prova:
      artefato: "micah build 36412b9 (= HEAD E6, commit da demolicao)"
      verificado_em: "src-tauri/target/release/micah.exe 9640960 bytes de 2026-08-17 21:20:07 -0300, build verde em 3m00s (log micah-tauri-build.log)"
      saida: "[2026-08-18][00:22:25][micah_lib][INFO] micah build 36412b9 no log da instancia viva com stdout redirecionado; cidade com 4 cores + legenda capturada em .proofs-micahs-mind/15-criterion1-final.png; seletor com label Micah's Mind em 21-criterion5-label.png; mode.test.ts 10/10 (coerce mantem ai-viewer)"
    achados:
      - categoria: "Instrumentacao"
        etapa: E2
        evidencia: "pane de aba ressuscitada rodou PowerShell SEM o profile de shell-integration (Test-Path Function:claude False em .proofs-micahs-mind/31-diag-function.png, claude 'nao reconhecido') com profile.ps1 correto em ~/.cache/micah/shell-integration/ e sem warn no log — a jornada de ancora claude-numa-aba quebra nesse caminho de spawn; pendente raiz (nao era o caso do pty do boot)"
      - categoria: "Estado e persistencia"
        etapa: E4
        evidencia: "clicar numa aba SEM sessao ancorada mantem a cidade da unica ancorada (crop-hdr-24.png identico a crop-hdr-27.png) — fallback E4 funcionando como especificado; criterio 4 exige duas ancoras distintas"
      - categoria: "Falha externa"
        etapa: prova
        evidencia: "window-click/window-drag falham SILENCIOSAMENTE quando o micah nao esta em primeiro plano (SetForegroundWindow bloqueado por protecao de foco): clique reporta sucesso e cai em outra janela; adicionado activate-micah com verificacao GetForegroundWindow antes de qualquer input sintetico"
      - categoria: "Volume"
        etapa: prova
        evidencia: "PrintWindow dedupe por conteudo (CDN reaproveita URL de byte-identico) virou detector de no-op: recorte pos-zoom identico ao pos-pan provou que o wheel nao registrou antes de se gastar shot"

  - card: explorer-raiz-livre-nas-2026-08-15
    titulo: "Explorer com raiz livre: navegar ate o NAS, com a lupa junto"
    data: 2026-08-15T09:20:00-03:00
    resultado: "done — header do explorer vira navegador (Home/subir/drives), raiz troca para Z: e lista o NAS, busca roda sem congelar a UI (7 comandos fs em spawn_blocking), junctions Hidden+System do perfil somem da arvore (os_hidden)"
    prova:
      artefato: "micah build f8b0bea-dirty (posterior a eaa6899, o commit do fix do explorer; o -dirty era o WIP das abas, commitado em 54efdef)"
      verificado_em: "src-tauri/target/release/micah.exe em execucao no zig-laptop, PID 34488 iniciado 2026-08-15T08:47:27-03:00"
      saida: "[2026-08-15][11:47:27][micah_lib][INFO] micah build f8b0bea-dirty (Micah.log da instancia viva) + captura docs/proof/explorer-root/n4-live-validation.png"
    achados:
      - categoria: "Falha externa"
        etapa: 1
        evidencia: "fs_list_drives sincrono sondava A:..Z: com is_dir() na main thread; drive de rede morto trava o redirector SMB por dezenas de segundos — trocado por GetLogicalDrives (bitmask local) + comando async"
      - categoria: "Concorrencia"
        etapa: 2
        evidencia: "7 comandos de fs sincronos rodavam inline no handler de IPC (janela Not Responding em busca no NAS) e o cancelamento por geracao do grep interativo era inerte; viraram async/spawn_blocking e generation virou Arc<AtomicU64> com bump antes do spawn"
      - categoria: "Volume"
        etapa: 2
        evidencia: "walk do Z:\\ inteiro via SMB/Tailscale medido em ~297 entradas/s; teto de 50k trunca em ~3min e 'No matches' truncado nao e prova de ausencia — mensagem de truncamento reescrita"
      - categoria: "Estado e persistencia"
        etapa: 2
        evidencia: "usar o retorno canonico do authorize como override trocava Z:/ pela grafia UNC (duas grafias do mesmo lugar, quebra 'subir'); autoriza pelo canonico mas o override guarda a grafia escolhida"
      - categoria: "Rollback"
        etapa: 2
        evidencia: "HEAD f32fe05 nao era autocontido (lib.rs referenciava fs_list_drives que so existia no worktree); o commit do card incluiu o WIP do Rodrigo"
      - categoria: "Entrada vazia ou suja"
        etapa: 3
        evidencia: "10 junctions Hidden+System+ReparsePoint do perfil (ACL de negacao por design) apareciam na arvore porque tree.rs so filtrava ponto-prefixo; os_hidden() passa a respeitar FILE_ATTRIBUTE_HIDDEN, teste windows_hidden_attribute_follows_show_hidden trava a invariante"
  - card: browser-pdi-2026-08-14
    titulo: "PDI do Browser: largura + favoritos + sanduiche (COM liberado)"
    data: 2026-08-14T23:50:00-03:00
    resultado: "done — largura 560 estavel (3 causas-raiz), rail de favoritos com favicon COM, sanduiche com historico/extensoes/limpar dados; aprovado pelo Rodrigo em teste manual ao vivo"
    prova:
      artefato: "micah build 07c9464-dirty (base 07c9464 + mudancas do card, commitadas em seguida como o fechamento deste registro)"
      verificado_em: "src-tauri/target/release/micah.exe em execucao no zig-laptop"
      saida: "[2026-08-15][02:44:56][micah_lib][INFO] micah build 07c9464-dirty + CDP url=https://volur.com.br/lobo apos clique no favorito"
    achados:
      - categoria: "Estado e persistencia"
        etapa: 1
        evidencia: "telemetria plugin-log: target=1600 com storage limpo; resize contra grupo de 157px grava grow=206 — guarda GROUP_SANE_MIN_WIDTH=860 no enforce e no persist (useBrowserPanel.ts)"
      - categoria: "Falha externa"
        etapa: 1
        evidencia: "resize() e malha aberta: um tiro aterrissava em 437/716/1313 (log); enforcement virou malha fechada age-confere-repete ate 8x"
      - categoria: "Entrada vazia ou suja"
        etapa: 1
        evidencia: "onPointerDown do React no separador de 1px nunca dispara para clique 1-3px ao lado; hit-test proprio no window capture (borda direita do painel ±8px)"
      - categoria: "Regressao"
        etapa: 3
        evidencia: "sanduiche abria ATRAS do webview: popper Radix dimensiona por mutacao de ATRIBUTO, observer childList nao ve (correcao 15 da auditoria, confirmada em producao pelo Rodrigo); fonte browser-menu por onOpenChange"
      - categoria: "Concorrencia"
        etapa: 2
        evidencia: "browser_page_info le url+titulo+favicon numa viagem COM so (correcao 9); persist sincrono no pointerup ANTES de liberar o enforcement (o enforcement desfazia o arrasto do usuario, medido)"
      - categoria: "Segredo em claro"
        etapa: 3
        evidencia: "redactUrl na query + userinfo, aplicada a historico, favoritos e URL_KEY do restore; 16 testes vitest (collections.test.ts)"
      - categoria: "Rollback"
        etapa: 2
        evidencia: "todos os comandos novos em dupla panel.rs+stub.rs; cargo check --no-default-features exit 0; cada cast COM e if-let (runtime velho degrada, nunca derruba)"
