import type { Theme } from "../types";

export const micahDefault: Theme = {
  id: "micah-default",
  name: "Micah Default",
  description: "The default Micah look — clean glass over neutral surfaces.",
  editorTheme: { dark: "atomone", light: "atomone" },
  variants: {
    light: {},
    dark: {},
  },
};
